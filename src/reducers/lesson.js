const prefix = ''

export const actionTypes = {}

const initState = {}

export default function lessonReducer(state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}
